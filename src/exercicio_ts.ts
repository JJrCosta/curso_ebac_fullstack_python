const calculaArea = (base: number, altura: number): number => base * altura;
console.log(`A área total é de: ${calculaArea(10, 8)}`);

const saucacao = (nome: string): string => `Olá, ${nome}!`;
console.log(saucacao('Junior'));